package main.scala.euler

object P004 {

  /**
    * Euler Problem 4. 
    * @param max
    * @return
    */
  def largestPalindromeProduct(max: Int): Int = {
    var largest = max
    for (a <- max to max / 10) {
      for (b <- max to max / 10) {
        //TODO:
      }
    }
    0
  }
}
